# @clite/compile

简单：珍贵、高贵

在 deno.json 中:

``` jsonc
{
	// ...
  "tasks": {
    "compile": "deno run -A jsr:@clite/compile --input=mod.ts --output=.out"
  },
  // ...
}
```

##### clite doctrine

仅保留一个 entry。
如果你觉得一个 entry 不够用，那么说明你的 app 不简单。
